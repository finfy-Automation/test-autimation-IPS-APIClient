name: Tests Run
on:
   push:
    branches:
      - 'main'
jobs:
  test-run-1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Tests run
        with:
          command: npm run test
